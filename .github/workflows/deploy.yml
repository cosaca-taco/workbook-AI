name: Deploy Public Folder to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # 🔒 ログにキーを漏らさず、安全にファイルを自動生成する正しいコマンド
      - name: Create Env Script
        env:
          MY_GEMINI_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          echo "window.GEMINI_API_KEY='${MY_GEMINI_KEY}';" > ./public/env.js

      - name: Inject script tag to HTML
        run: |
          sed -i 's|<script src="app.js"></script>|<script src="env.js"></script><script src="app.js"></script>|g' ./public/index.html

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './public'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4